<?xml version="1.0" encoding="utf-8"?>
<!-- Only the export directory is shareable. cacheDir stays out of the
     provider on purpose: it also holds the transient photo the system call
     directory reads, which no other app has any business seeing. -->
<paths>
    <files-path name="shared" path="shared_exports/" />
</paths>
